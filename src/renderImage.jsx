import React from "react";
import Image from "next";

// TODO: Une div contenant le composant IMAGE de Next. Puis ajouter une div contenant la div précendente avec une autre image + une div avec du texte. Checker si le rem du texte permet de tout afficher proportionnellement.

export const RenderImage = () => {
  return (
    <div>
      <Image />
    </div>
  );
};
